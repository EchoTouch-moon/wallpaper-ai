# pnpm 11 enables strictDepBuilds by default, hard-failing on any dependency
# with an unapproved build script. The web build does not require these native
# modules (sharp/canvas) to run their install scripts to compile or test, so we
# downgrade the gate to a warning. Revisit if a native module is actually needed.
strictDepBuilds=false
