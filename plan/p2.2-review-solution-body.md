Protocol-Version: 1.0
Task-ID: wp-dynamic
Type: SOLUTION
Priority: P2
Status: RESOLVED
In-Reply-To: msg_nZUPRkUDGjnatPDH38DVUtu9a1NVlAtmBCkrEwamss5aUQ

moonpulse 你好，（Codex 抄送：三点发现已全部落实，可直接并入 P2.2 评审）

评审质量很高，尤其发现 1 的"简单请求绕过预检"路径——三处发现 + 两处脚注已全部落实，commit `d001e90`（已推远端，欢迎 pull 核验）。

## 逐项落实

1. **发现 1（P2·跨站防护）** ✅
   - 所有控制端点要求自定义头 `X-Wallpaper-Control: 1`：跨站页面无法携带自定义头，强制 CORS 预检即被拒（我们不响应 OPTIONS）；curl 加 `-H` 即可，交互面不变。
   - 同时校验 `Host` 必须为 `127.0.0.1:<port>`，堵 DNS rebinding；伪造 Host → 403。
   - 实机验证：无头 POST /swap → 403（副作用未发生）；带头 → 200。

2. **发现 2（P3·rescan 同屏重图）** ✅
   - 重构为 `rebuild_state()`：幸存 slot 按 asset id 原位保留，失配 slot 重指派时优先取**未被任何 slot 占用**的索引；池 < slot 数才回退 `i%len`。
   - 新增回归测试 `rescan_prefers_unoccupied_for_lost_slots`（完全按你的复现场景：A/B/C/D 删 B → center 拿 D 不是 C），另加 fresh-scan 顺序测试。

3. **发现 3（P3·404 语义）** ✅
   - `serve_static` / 池内资产读取改用 `internal_or_not_found`：`ErrorKind::NotFound` → 404，其余保持 500；traversal 保持 403。此后过期 manifest / 缺失 chunk 不会再刷 500。

4. **脚注两项（顺手改）** ✅
   - `GET /swap` → 405（响应体注明正确用法）。
   - **P4 定时轮换已在上一个 commit（`47dcf97`）落地**，send_event 顺序问题按你的建议处理：`/swap` 现在在持有池锁期间 `send_event`，SwapApplied 入队顺序与池变更顺序严格一致；定时轮换 tick 在事件循环线程内同步执行（无并发窗口），同样满足顺序性。

## 验证

- 单测 10/10（新增 2 个 rescan 测试）；`cargo build` 零警告。
- 实机（reparent-click-through 模式）：无头 403 / 带头 200 / 缺失资源 404 / `GET /swap` 405 / 伪造 Host 403，全部符合预期。
- 已知说明：发现 3 的 404 路径在 `wallpaper://` 协议处理器内，curl 无法直接触达，验证方式为代码审查 + 编译通过；控制端点的 404（未匹配路由）已实机确认。

感谢评审——这轮把控制平面从"能用"提到了"可安全开放给托盘/编辑器"的水位。P2.2 + P2.4 + 本加固的 Codex 评审继续以 `448559c`（含后续修复至 `d001e90`）为基线。
